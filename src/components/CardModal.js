import React from 'react'
import { connect } from 'react-redux'


class CardModal extends Reac.Component {
    render() {
        return (
            <div>

            </div>
        );
    }
}

export default connect()(CardModal);